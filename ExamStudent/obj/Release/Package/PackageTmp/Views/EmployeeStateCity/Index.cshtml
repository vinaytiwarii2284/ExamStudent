@model IEnumerable<ExamStudent.Models.EmpState>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Emp_State)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Emp_State)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmpStateID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmpStateID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmpStateID })
        </td>
    </tr>
}

</table>
